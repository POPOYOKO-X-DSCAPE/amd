import { css } from "@styles";

export const styles = {
	fluxImg: css({
		_desktop: {
			maxWidth: "60%",
		},
	}),
	fluxText: css({
		maxWidth: "70ch",
		textStyle: "body.s",
	}),
	fluxButton: css({
		_desktop: {
			maxWidth: "30%",
		},
	}),
};
