import { App as AbstractApp } from "@packages/ui";
import { Button } from "@packages/ui/components/button";

const App = () => {
  return (
    <AbstractApp>
      <Button>as</Button>
    </AbstractApp>
  );
};

export default App;
